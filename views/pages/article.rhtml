<article class="post">
  <header>
    <h1><%= article.title %></h1>
    <span class="date"><%= Date.parse(article.date).strftime("%B %e, %Y") %></span>
  </header>
  <section class="content">
    <%= article.content %>
  </section>
</article>

